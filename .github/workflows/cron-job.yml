name: Schedule Cron Job

on:
  schedule:
    - cron: "* * * * *"

jobs:
  call-vercel-api:
    runs-on: ubuntu-latest

    steps:
      # - name: Call Vercel API
      #   run: node index.js
      - name: execute
        run: echo "hello"
