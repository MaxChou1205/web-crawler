name: Schedule Cron Job

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  call-vercel-api:
    runs-on: ubuntu-latest

    steps:
      - name: Call Vercel API
        run: curl -X GET https://web-crawler-sigma.vercel.app/cron-job.js
