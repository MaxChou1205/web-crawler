name: Schedule Cron Job

on:
  schedule:
    - cron: "* * * * *"

jobs:
  call-vercel-api:
    runs-on: ubuntu-latest

    steps:
    - name: Call Vercel API
      run: curl -X GET https://web-crawler-sandy.vercel.app/yungching