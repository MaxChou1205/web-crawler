name: Schedule Cron Job

on:
  schedule:
    - cron: "* * * * *"

jobs:
  call-vercel-api:
    runs-on: ubuntu-latest

    steps:
      # - name: Call Vercel API
      #   run: node index.js
      - uses: actions/checkout@v2
      - name: execute
        uses: MaxChou1205/web-crawler@master
